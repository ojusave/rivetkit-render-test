import { SqliteRemoteDatabase } from 'drizzle-orm/sqlite-proxy';
import { D as DatabaseProvider, R as RawAccess } from '../../config-BiNoIHRs.js';
export * from 'drizzle-orm/sqlite-core';
export { blob, check, extractUsedTable, foreignKey, getTableConfig, getViewConfig, index, integer, numeric, primaryKey, real, sqliteTable, sqliteTableCreator, sqliteView, text, unique, uniqueIndex, uniqueKeyName, view } from 'drizzle-orm/sqlite-core';
import { Config } from 'drizzle-kit';
import '@rivetkit/sqlite-vfs';

declare function defineConfig(config: Partial<Config & {
    driver: "durable-sqlite";
}>): Config;
interface DatabaseFactoryConfig<TSchema extends Record<string, unknown> = Record<string, never>> {
    schema?: TSchema;
    migrations?: any;
}
declare function db<TSchema extends Record<string, unknown> = Record<string, never>>(config?: DatabaseFactoryConfig<TSchema>): DatabaseProvider<SqliteRemoteDatabase<TSchema> & RawAccess>;

export { db, defineConfig };
